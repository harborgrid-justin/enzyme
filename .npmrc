# @defendr/enzyme npm configuration

# Registry configuration
@defendr:registry=https://registry.npmjs.org/

# Publishing settings
access=public
save-exact=false

# Security
package-lock=true

# Authentication - use environment variable NPM_TOKEN
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
